app:
  version: '@project.version@'

# Para exponer los endpoints de actuator
management:
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: health,info,prometheus,env
      cors:
        allowed-origins: "http://localhost"
        allowed-methods: "GET,POST"
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
    prometheus:
      enabled: true
  metrics:
    export:
      prometheus:
        enabled: true

# Datos del server
server:
  port: 8080
  servlet:
    context-path: "/${spring.application.name}-api-v${app.version_url}" 
  ssl:
    enabled: false

# Datos de spring
spring:
  banner:
    location: classpath:/banner.txt
  main:
    banner-mode: console
  # profiles:
  #  active: des
  output:
    ansi:
      enabled: DETECT
# Seguridad
  security:
    enable-csrf: false
    enabled: false
    basic:
      enabled: false

#elasticsearch
elasticsearch:
  host: 10.80.54.4
  authorization: Basic '{cipher}853c8cfe2cee03370d779f0b2ff1967baacc13ff44924c1f88039ca7918f836a7e3dc38f0b74595cca621a1f20d12fcee1e1c6a3c2e6b6b5206ed6516a7a79e023b3ba11d042361663396258911c5803'
  timeout: 30000
  
# jasypt:
#   encryptor:
#     algorithm: PBEWithMD5AndDES
#     keyObtentionIterations: 1000
#     poolSize: 1
#     providerName: SunJCE
#     saltGeneratorClassname: org.jasypt.salt.RandomSaltGenerator
#     iv-generator-classname: org.jasypt.iv.NoIvGenerator
#     stringOutputType: base64
#     password: BylArqzrx2022Abr

global-message: Configuration General entorno Des! (fichero application.yml)
arqrama-message: Mensaje general.
